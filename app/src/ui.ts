import{POWERED_BY_BGG_LOGO}from"./bgg-logo";

export type AppUser = { email: string; role: string };
const nav = [["library","Library"],["missing-prices","Missing prices"],["import","Import"],["trades","Trades"],["matcher","List matcher"],["account","Account"]] as const;
const descriptions:Record<string,string>={library:"Your private copy data: costs, locations, condition, notes, and trade status.","missing-prices":"Resolve copies that do not yet have a known acquisition cost.",import:"Review a BoardGameGeek CSV before it changes any private copy data.",trades:"Record outgoing and incoming games while carrying acquisition cost through the trade.",matcher:"Paste game names or BoardGameGeek links and match them against your imported library.",account:"Export your data, review account activity, or manage account deletion.",invitations:"Invite people into the private BoardGameEngine preview."};
export const escapeHtml=(v:string)=>v.replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"})[c]??c);

export function signInPage(message=""){return page("Sign in",`<main class="auth"><a class="brand auth-brand" href="https://boardgames.planitnow.us/"><span class="die" aria-hidden="true">••<br>••</span><strong>BoardGame<span>Engine</span></strong></a><section class="card"><p class="section-label">Sign in</p><h1>Welcome back</h1><p>Use the email address that received your invitation.</p>${message?`<aside>${escapeHtml(message)}</aside>`:""}<form method="post" action="/auth/request"><label>Email address<input name="email" type="email" autocomplete="email" required></label><button>Email me a sign-in link</button></form><details><summary>I have a sign-in code</summary><form method="post" action="/auth/code"><label>Email address<input name="email" type="email" required></label><label>Sign-in code<input name="code" autocomplete="one-time-code" placeholder="ABCD-2345" required></label><button>Sign in with code</button></form></details></section><p class="note">Opening an emailed link does not use it. You confirm before signing in.</p></main>`)}
export function confirmPage(token:string){return page("Confirm sign-in",`<main class="auth"><a class="brand auth-brand" href="https://boardgames.planitnow.us/"><span class="die" aria-hidden="true">••<br>••</span><strong>BoardGame<span>Engine</span></strong></a><section class="card"><p class="section-label">Confirm sign-in</p><h1>One final step</h1><p>This link has not been consumed. Continue only if you requested it.</p><form method="post" action="/auth/confirm"><input type="hidden" name="token" value="${escapeHtml(token)}"><button>Continue signing in</button></form></section></main>`)}

export function appPage(user:AppUser,section="library"){
 const title=nav.find(x=>x[0]===section)?.[1]??(section==="invitations"?"Invitations":"Library");
 const links=nav.map(([s,l])=>`<a href="/app/${s}"${s===section?' aria-current="page"':""}>${l}</a>`).join("")+(user.role==="admin"?`<a href="/app/invitations"${section==="invitations"?' aria-current="page"':""}>Invitations</a>`:"");
 const content=sectionContent(section,title,user);
 const accountCurrent=section==="account"?' aria-current="page"':"";
 return page(title,`<header class="topbar"><div class="topbar-inner"><a class="brand" href="/app"><span class="die" aria-hidden="true">••<br>••</span><strong>BoardGame<span>Engine</span></strong></a><nav class="primary-nav" aria-label="Application"><a href="/app">Tonight</a><a href="/app/library"${section!=="account"?' aria-current="page"':""}>Library</a><a href="/app/account"${accountCurrent}>Account</a></nav><form action="/auth/logout" method="post"><button class="quiet" type="submit">Sign out</button></form></div></header><main class="app-shell"><section class="page-head"><p class="section-label">Library tools</p><h1>${escapeHtml(title)}</h1><p>${escapeHtml(descriptions[section]??descriptions.library)}</p></section><nav class="tool-tabs" aria-label="Library tools">${links}</nav>${content}</main><footer class="app-footer"><div><strong>${escapeHtml(user.email)}</strong><p>Private library tools for BoardGameEngine.</p></div><a class="bgg-credit" href="https://boardgamegeek.com/" target="_blank" rel="noopener noreferrer"><img src="${POWERED_BY_BGG_LOGO}" alt="Powered by BoardGameGeek"></a></footer>`)}

function sectionContent(section:string,title:string,user:AppUser){if(section==="library")return`<section class="metrics" id="metrics"></section><section class="surface tool library"><div class="toolbar"><input id="library-search" type="search" placeholder="Search title or BGG ID" aria-label="Search library"><select id="library-filter" aria-label="Filter library"><option value="all">All active copies</option><option value="missing">Missing price</option><option value="for-trade">For trade</option><option value="wishlist">Wishlist</option></select><select id="library-sort" aria-label="Sort library"><option value="name">Title</option><option value="cost-desc">Highest cost</option><option value="updated">Recently edited</option></select></div><p class="status-line" id="library-status" role="status"></p><div id="library-list">Loading collection…</div></section>`;if(section==="import")return tool(`<div class="tool-intro"><h2>Import a collection CSV</h2><p>Preview every recognized copy before saving. Existing costs and possible removals always require a separate decision.</p></div><div id="cost-review-list"></div><div id="removal-list"></div><form id="import-form"><label>BoardGameGeek collection CSV<input name="file" type="file" accept=".csv,text/csv" required></label><button>Review import</button></form><div id="import-result"></div><div class="section-break"><h3>Import history</h3><div id="import-history">Loading…</div></div>`);if(section==="missing-prices")return tool(`<div class="tool-intro"><h2>Price the remaining copies</h2><p>Unknown prices stay distinct from explicit zero-cost copies.</p></div><div id="missing-list">Loading…</div>`);if(section==="invitations")return tool(`<div class="tool-intro"><h2>Invite a collector</h2><p>Invitations are tied to an email address and can be revoked or resent.</p></div><form id="invite-form" class="inline-form"><label>Email address<input name="email" type="email" required></label><button>Send invitation</button></form><div class="section-break" id="invite-list">Loading…</div>`);if(section==="trades")return tool(`<div class="tool-intro"><h2>Record a trade</h2><p>Outgoing copy cost is allocated across incoming games so your private acquisition history stays intact.</p></div><form id="trade-form"><fieldset><legend>Trade details</legend><div class="form-grid two"><label>Counterparty<input name="counterparty" maxlength="500"></label><label>Trade date<input name="tradedOn" type="date"></label></div><label>Notes<textarea name="notes" rows="2" maxlength="2000"></textarea></label></fieldset><fieldset><legend>Outgoing copies</legend><label>Search copies<input id="copy-search" type="search"></label><div id="copy-list">Loading owned copies…</div></fieldset><fieldset><legend>Incoming games</legend><div id="incoming-list"><div class="incoming"><input name="bggId" type="number" min="1" placeholder="BGG ID" required><input name="title" placeholder="Title" required><input name="weight" type="number" min=".01" step=".01" value="1" aria-label="Relative weight"></div></div><button class="secondary-button" id="add-incoming" type="button">Add another game</button></fieldset><div class="form-grid two"><label>Shipping<input name="shipping" type="number" min="0" step=".01" value="0"></label><label>Additional cash<input name="cash" type="number" min="0" step=".01" value="0"></label></div><button>Review and record allocation</button></form><div id="trade-result" role="status"></div><div class="section-break"><div class="split-heading"><h3>Trade history</h3><p><a href="/api/trades/export">Markdown</a> · <a href="/api/trades/export?format=text">Text</a> · <a href="/api/trades/export?format=csv">CSV</a></p></div><div id="trade-history">Loading…</div></div>`);if(section==="picker")return tool(`<div class="tool-intro"><h2>The picker moved to Tonight</h2><p>The live BGG-backed picker is now the main BoardGameEngine experience.</p></div><p><a class="button" href="/app">Open Tonight</a></p>`);if(section==="matcher")return tool(`<div class="tool-intro"><h2>Match a pasted list</h2><p>Use game names or BoardGameGeek links. Results are matched against games already imported into your library.</p></div><form id="matcher"><label>Game names or BGG links<textarea name="lines" rows="10" required></textarea></label><button>Match list</button></form><div id="match-result"></div>`);if(section==="account")return tool(`<div class="account-actions"><div><h2>Your data</h2><p>Download a copy of the private data BoardGameEngine stores for this account.</p></div><a class="button" href="/api/account/export">Download my data</a></div><div class="section-break"><h3>Recent account activity</h3><div id="audit-list">Loading…</div></div>${user.role==="admin"?`<p class="warning">The sole administrator account cannot be deleted. Transfer administration first.</p>`:`<div class="danger-zone"><h3>Delete account</h3><p>This permanently removes your BoardGameEngine account and private app data.</p><form id="delete-account"><label>Confirm your email<input name="confirmEmail" type="email" required></label><button class="danger-button">Delete my account</button></form></div>`}`);return`<section class="surface"><h2>${escapeHtml(title)} is next.</h2></section>`}
const tool=(inside:string)=>`<section class="surface tool">${inside}</section>`;
function page(title:string,body:string){return`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${escapeHtml(title)} — BoardGameEngine</title><style>${css}</style></head><body>${body}<script>${client}</script></body></html>`}
const client=`const q=s=>document.querySelector(s),esc=s=>String(s).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c])),api=async(u,o={})=>{const r=await fetch('/api'+u,o),j=await r.json();if(!r.ok)throw Error(j.error||'Request failed');return j};
const money=n=>n===null?'Unknown':(Number(n)/100).toFixed(2);const loadMetrics=()=>q('#metrics')&&api('/dashboard').then(d=>{const s=d.summary,c=Number(s.copies||0),p=Number(s.priced||0);q('#metrics').innerHTML='<article><small>OWNED COPIES</small><b>'+c+'</b><span>active</span></article><article><small>KNOWN COST</small><b>'+money(Number(s.total||0))+'</b><span>known total</span></article><article><small>PRICE COVERAGE</small><b>'+(c?Math.round(p/c*100):0)+'%</b><span>'+p+' priced</span></article>'});loadMetrics();
if(q('#library-list')){const loadLibrary=()=>{const p=new URLSearchParams({search:q('#library-search').value,filter:q('#library-filter').value,sort:q('#library-sort').value});api('/copies?'+p).then(rows=>{q('#library-status').textContent=rows.length+' active '+(rows.length===1?'copy':'copies');q('#library-list').innerHTML=rows.length?rows.map(c=>'<details class="copy-card"><summary><span><b>'+esc(c.name)+'</b><small> BGG '+c.bggId+(c.year?' · '+c.year:'')+'</small></span><strong>'+money(c.costMinor)+'</strong></summary><form class="copy-edit" data-id="'+esc(c.id)+'"><div class="form-grid"><label>Cost<input name="cost" type="number" min="0" step=".01" value="'+(c.costMinor===null?'':c.costMinor/100)+'"></label><label>Location<input name="location" maxlength="2000" value="'+esc(c.inventoryLocation||'')+'"></label><label>Condition<input name="condition" maxlength="2000" value="'+esc(c.conditionText||'')+'"></label><label>Acquired from<input name="acquiredFrom" maxlength="2000" value="'+esc(c.acquiredFrom||'')+'"></label></div><label>Private note<textarea name="privateComment" maxlength="2000">'+esc(c.privateComment||'')+'</textarea></label><label class="check"><input name="forTrade" type="checkbox" '+(c.forTrade?'checked':'')+'> Offered for trade</label><button>Save copy</button> <output></output></form></details>').join(''):'<p>No copies match these filters.</p>';document.querySelectorAll('.copy-edit').forEach(f=>f.onsubmit=async e=>{e.preventDefault();const d=Object.fromEntries(new FormData(f));d.forTrade=f.forTrade.checked;d.cost=d.cost||null;await api('/copies/'+encodeURIComponent(f.dataset.id),{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify(d)});f.querySelector('output').textContent='Saved';loadMetrics()})})};let timer;['#library-filter','#library-sort'].forEach(s=>q(s).onchange=loadLibrary);q('#library-search').oninput=()=>{clearTimeout(timer);timer=setTimeout(loadLibrary,200)};loadLibrary()}
if(q('#import-history'))api('/imports').then(x=>q('#import-history').innerHTML=x.length?x.map(r=>'<article class="report"><b>'+esc(r.createdAt)+'</b><span>'+r.rowCount+' rows · '+r.changedCosts+' changed costs · '+r.possibleRemovals+' possible removals</span></article>').join(''):'<p>No completed imports.</p>');
if(q('#cost-review-list')){const loadCosts=()=>api('/cost-reviews').then(x=>q('#cost-review-list').innerHTML=x.length?'<h3>Changed prices to review</h3>'+x.map(r=>'<p>'+esc(r.name)+': '+(r.existingCostMinor/100).toFixed(2)+' → '+(r.importedCostMinor/100).toFixed(2)+' <button class="cost-review" data-id="'+esc(r.id)+'" data-action="keep">Keep current</button> <button class="cost-review" data-id="'+esc(r.id)+'" data-action="accept-import">Use imported</button></p>').join(''):'');loadCosts();q('#cost-review-list').onclick=async e=>{const b=e.target.closest('.cost-review');if(!b)return;await api('/cost-reviews/'+b.dataset.id,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:b.dataset.action})});loadCosts()}}
if(q('#removal-list')){const loadRemovals=()=>api('/removal-reviews').then(x=>q('#removal-list').innerHTML=x.length?'<h3>Possible removals</h3>'+x.map(r=>'<p>'+esc(r.name)+' <button class="removal" data-id="'+esc(r.id)+'" data-action="keep">Keep copy</button> <button class="removal" data-id="'+esc(r.id)+'" data-action="remove">Mark no longer owned</button></p>').join(''):'');loadRemovals();q('#removal-list').onclick=async e=>{const b=e.target.closest('.removal');if(!b)return;await api('/removal-reviews/'+b.dataset.id,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:b.dataset.action})});loadRemovals()}}
if(q('#import-form'))q('#import-form').onsubmit=async e=>{e.preventDefault();const d=await api('/imports/preview',{method:'POST',body:new FormData(e.target)});window.preview=d.rows;const multi=d.rows.map((r,i)=>({r,i})).filter(x=>x.r.quantity>1);q('#import-result').innerHTML='<p>'+d.summary.rows+' rows · '+d.summary.copies+' copies · '+d.summary.unknownCosts+' unknown costs · '+d.summary.warnings+' warnings</p>'+multi.map(x=>'<label>'+esc(x.r.name)+' has quantity '+x.r.quantity+'<select class="quantity-choice" data-index="'+x.i+'"><option value="">Choose…</option><option value="one">Import one copy</option><option value="split">Split into '+x.r.quantity+' copies</option></select></label>').join('')+'<button id="confirm-import" '+(d.rows.some(r=>r.warnings.some(w=>!w.includes('Quantity above')))?'disabled':'')+'>Confirm import</button><pre>'+esc(JSON.stringify(d.rows.slice(0,20),null,2))+'</pre>';q('#confirm-import')?.addEventListener('click',async()=>{let rows=[];for(const r of window.preview){if(r.quantity>1){const choice=q('[data-index="'+window.preview.indexOf(r)+'"]').value;if(!choice)return alert('Choose how to handle every extra quantity.');if(choice==='one')rows.push({...r,quantity:1,warnings:[]});else for(let n=0;n<r.quantity;n++)rows.push({...r,collId:r.collId+'-'+(n+1),quantity:1,priceMinor:r.priceMinor===null?null:Math.floor(r.priceMinor/r.quantity)+(n<r.priceMinor%r.quantity?1:0),warnings:[]})}else rows.push(r)}const button=q('#confirm-import');button.disabled=true;try{let runId;for(let i=0;i<rows.length;i+=10){button.textContent='Importing '+Math.min(i+10,rows.length)+' of '+rows.length+'…';const result=await api('/imports/confirm',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({rows:rows.slice(i,i+10),runId,totalRows:rows.length,final:i+10>=rows.length})});runId=result.run}button.textContent='Import complete';location='/app'}catch(error){button.disabled=false;button.textContent='Try import again';q('#import-result').insertAdjacentHTML('afterbegin','<p class="warning" role="alert">Import stopped: '+esc(error.message)+'. No existing costs or ownership decisions were changed.</p>')}})};
if(q('#missing-list'))api('/missing-prices').then(rows=>{q('#missing-list').innerHTML=rows.length?rows.map(x=>'<form class="cost" data-id="'+esc(x.id)+'"><b>'+esc(x.name)+'</b><input name="amount" type="number" min="0" step=".01" required><input name="currency" placeholder="currency"><button>Save</button></form>').join(''):'<p>Nothing is waiting for a price.</p>';document.querySelectorAll('.cost').forEach(f=>f.onsubmit=async e=>{e.preventDefault();const o=Object.fromEntries(new FormData(f));await api('/copies/'+f.dataset.id+'/cost',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify(o)});f.remove()})});
if(q('#invite-form')){const load=()=>api('/invitations').then(x=>q('#invite-list').innerHTML=x.map(i=>'<p>'+esc(i.email)+' — '+i.status+(i.status==='pending'?' <button class="invite-action" data-id="'+i.id+'" data-action="resend">Resend</button> <button class="invite-action" data-id="'+i.id+'" data-action="revoke">Revoke</button>':'')+(i.disabledAt?' <button class="invite-action" data-id="'+i.id+'" data-action="reenable">Re-enable</button>':' <button class="invite-action" data-id="'+i.id+'" data-action="disable">Disable</button>')+'</p>').join(''));load();q('#invite-form').onsubmit=async e=>{e.preventDefault();await api('/invitations',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});e.target.reset();load()};q('#invite-list').onclick=async e=>{const b=e.target.closest('.invite-action');if(!b)return;await api('/invitations/'+b.dataset.id,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:b.dataset.action})});load()}}
if(q('#audit-list'))api('/account/audit').then(x=>q('#audit-list').innerHTML=x.length?x.map(e=>'<p>'+esc(e.createdAt)+' — '+esc(e.eventType)+'</p>').join(''):'<p>No recorded activity yet.</p>');
if(q('#delete-account'))q('#delete-account').onsubmit=async e=>{e.preventDefault();if(!confirm('Permanently delete this account and its BoardGameEngine data?'))return;await api('/account',{method:'DELETE',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});location='/'};
if(q('#matcher'))q('#matcher').onsubmit=async e=>{e.preventDefault();const d=await api('/match',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});q('#match-result').innerHTML='<pre></pre>';q('#match-result pre').textContent=JSON.stringify(d,null,2)};
if(q('#picker'))q('#picker').onsubmit=async e=>{e.preventDefault();const f=Object.fromEntries(new FormData(e.target)),candidates=[{id:1,name:'Fixture Game',minPlayers:2,maxPlayers:4,minutes:60,weight:2.5,best:20,recommended:25,notRecommended:5},{id:2,name:'Divisive Game',minPlayers:1,maxPlayers:6,minutes:45,weight:2,best:12,recommended:8,notRecommended:20}];const d=await api('/recommend',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({players:+f.players,minutes:+f.minutes,minWeight:0,maxWeight:5,candidates})});q('#picker-result').innerHTML=d.map(x=>'<h3>'+esc(x.name)+'</h3><p>'+esc(x.reason)+'</p>'+(x.warning?'<p class="warning">⚠ '+esc(x.warning)+'</p>':'')).join('')};
if(q('#trade-history')){const loadTrades=()=>api('/trades').then(rows=>q('#trade-history').innerHTML=rows.length?rows.map(t=>'<article class="report"><b>'+(t.tradedOn||t.createdAt.slice(0,10))+(t.counterparty?' · '+esc(t.counterparty):'')+'</b><span>Shipping '+money(t.shippingMinor)+' · cash '+money(t.cashMinor)+(t.reversedAt?' · Reversed':'')+'</span><button class="trade-detail" data-id="'+esc(t.id)+'">Details</button>'+(t.reversedAt?'':' <button class="trade-reverse" data-id="'+esc(t.id)+'">Reverse</button>')+'<div id="trade-'+esc(t.id)+'"></div></article>').join(''):'<p>No trades recorded.</p>');loadTrades();q('#trade-history').onclick=async e=>{const b=e.target.closest('button');if(!b)return;if(b.matches('.trade-detail')){const d=await api('/trades/'+encodeURIComponent(b.dataset.id));q('#trade-'+b.dataset.id).innerHTML='<ul>'+d.lines.map(x=>'<li>'+esc(x.direction==='out'?'Outgoing: ':'Incoming: ')+esc(x.title)+' — '+money(x.carriedCostMinor??x.allocatedCostMinor)+'</li>').join('')+'</ul>'}else if(b.matches('.trade-reverse')&&confirm('Reverse this trade and restore outgoing copies?')){await api('/trades/'+encodeURIComponent(b.dataset.id)+'/reverse',{method:'POST'});loadTrades();loadMetrics()}}}
if(q('#copy-list'))api('/copies').then(x=>q('#copy-list').innerHTML=x.length?x.map(c=>'<label class="copy-option"><input type="checkbox" name="outgoingCopy" value="'+esc(c.id)+'" '+(c.costMinor===null?'disabled':'')+'> '+esc(c.name)+' — '+(c.costMinor===null?'set cost first':(c.costMinor/100).toFixed(2))+'</label>').join(''):'<p>No active copies.</p>');
if(q('#copy-search'))q('#copy-search').oninput=e=>document.querySelectorAll('.copy-option').forEach(x=>x.hidden=!x.textContent.toLowerCase().includes(e.target.value.toLowerCase()));
if(q('#add-incoming'))q('#add-incoming').onclick=()=>q('#incoming-list').insertAdjacentHTML('beforeend','<div class="incoming"><input name="bggId" type="number" min="1" placeholder="BGG ID" required><input name="title" placeholder="Title" required><input name="weight" type="number" min=".01" step=".01" value="1"></div>');
if(q('#trade-form'))q('#trade-form').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.target),outgoing=[...document.querySelectorAll('[name=outgoingCopy]:checked')].map(x=>({copyId:x.value,costMinor:0,title:''})),incoming=[...document.querySelectorAll('.incoming')].map(x=>({bggId:+x.querySelector('[name=bggId]').value,title:x.querySelector('[name=title]').value,weight:+x.querySelector('[name=weight]').value||1}));const shippingMinor=Math.round(+f.get('shipping')*100),cashMinor=Math.round(+f.get('cash')*100);if(!confirm('Record this trade? Outgoing: '+outgoing.length+', incoming: '+incoming.length+', shipping/cash: '+((shippingMinor+cashMinor)/100).toFixed(2)))return;const d=await api('/trades',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({outgoing,incoming,shippingMinor,cashMinor,currency:'',notes:f.get('notes'),counterparty:f.get('counterparty'),tradedOn:f.get('tradedOn')})});q('#trade-result').textContent='Allocated total: '+(d.total/100).toFixed(2)+'\\nIncoming allocations: '+d.allocations.map(x=>(x/100).toFixed(2)).join(', ')};`;
const css=`
:root{--bg:#f3f1eb;--surface:#fdfcf8;--ink:#20231f;--muted:#6b6f68;--line:#d7d5ce;--line-strong:#bebcb5;--accent:#315744;--accent-soft:#e5ece7;--danger:#984b39;--focus:#8ba99a}
*{box-sizing:border-box}
html{background:var(--bg)}
body{margin:0;background:var(--bg);color:var(--ink);font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;font-size:15px;line-height:1.5}
a{color:inherit}
button,input,select,textarea{font:inherit}
button,.button{display:inline-flex;align-items:center;justify-content:center;min-height:40px;border:1px solid var(--accent);border-radius:8px;background:var(--accent);color:white;padding:9px 14px;font-weight:700;text-decoration:none;cursor:pointer}
button:hover,.button:hover{filter:brightness(.94)}
button:disabled{opacity:.55;cursor:not-allowed}
.secondary-button{background:transparent;color:var(--ink);border-color:var(--line-strong)}
.danger-button{background:var(--danger);border-color:var(--danger)}
.quiet{min-height:auto;background:transparent;border:0;color:var(--muted);padding:7px 0;font-weight:600}
.brand{display:flex;align-items:center;gap:9px;color:var(--ink);text-decoration:none;white-space:nowrap}.brand strong{font-size:17px;letter-spacing:-.025em}.brand strong span{color:var(--accent)}.die{font-size:9px;line-height:7px;background:var(--ink);color:white;border-radius:5px;padding:7px 7px;letter-spacing:3px}
.topbar{position:sticky;top:0;z-index:20;border-bottom:1px solid var(--line);background:rgba(243,241,235,.96);backdrop-filter:blur(10px)}.topbar-inner{max-width:1120px;min-height:62px;margin:0 auto;padding:0 22px;display:flex;align-items:center;gap:26px}.primary-nav{display:flex;align-items:center;gap:21px;flex:1}.primary-nav a{color:var(--muted);text-decoration:none;font-size:13px;font-weight:650;padding:21px 0 18px;border-bottom:2px solid transparent}.primary-nav a:hover,.primary-nav a[aria-current="page"]{color:var(--ink);border-bottom-color:var(--accent)}
.app-shell{max-width:1120px;margin:0 auto;padding:42px 22px 64px}.page-head{max-width:720px;margin-bottom:26px}.section-label{margin:0 0 7px;color:var(--accent);font-size:11px;font-weight:800;letter-spacing:.12em;text-transform:uppercase}.page-head h1,.auth h1{margin:0;color:var(--ink);font-size:36px;line-height:1.08;letter-spacing:-.035em}.page-head>p:last-child,.tool-intro p,.account-actions p{margin:11px 0 0;color:var(--muted);font-size:15px;line-height:1.6}.tool-tabs{display:flex;gap:2px;overflow:auto;margin:0 0 20px;border-bottom:1px solid var(--line);scrollbar-width:none}.tool-tabs::-webkit-scrollbar{display:none}.tool-tabs a{flex:0 0 auto;padding:10px 12px 11px;color:var(--muted);text-decoration:none;font-size:13px;font-weight:650;border-bottom:2px solid transparent}.tool-tabs a:hover,.tool-tabs a[aria-current="page"]{color:var(--ink);border-bottom-color:var(--accent)}
.surface{background:var(--surface);border:1px solid var(--line);border-radius:10px;padding:24px}.tool-intro{max-width:680px;margin-bottom:24px}.tool-intro h2,.account-actions h2{margin:0;font-size:20px;letter-spacing:-.02em}.tool h3{margin:0 0 12px;font-size:16px;letter-spacing:-.01em}.section-break{margin-top:28px;padding-top:24px;border-top:1px solid var(--line)}.split-heading,.account-actions{display:flex;align-items:flex-start;justify-content:space-between;gap:20px}.split-heading p{margin:0;color:var(--muted)}
.metrics{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin-bottom:12px}.metrics article{background:var(--surface);border:1px solid var(--line);border-radius:9px;padding:17px 18px}.metrics small,.metrics b,.metrics span{display:block}.metrics small{color:var(--muted);font-size:10px;font-weight:800;letter-spacing:.09em}.metrics b{margin-top:7px;font-size:28px;line-height:1;letter-spacing:-.03em}.metrics span{margin-top:5px;color:var(--muted);font-size:12px}
.tool form{display:grid;gap:14px}.tool label,.copy-option{display:block;font-size:13px;font-weight:700}.tool input,.tool textarea,.tool select,.card input{width:100%;margin-top:6px;padding:10px 11px;border:1px solid var(--line-strong);border-radius:7px;background:white;color:var(--ink);outline:none}.tool input:focus,.tool textarea:focus,.tool select:focus,.card input:focus{border-color:var(--focus);box-shadow:0 0 0 3px rgba(139,169,154,.18)}.tool textarea{resize:vertical}.tool fieldset{margin:0;border:1px solid var(--line);border-radius:8px;padding:18px}.tool legend{padding:0 6px;font-size:13px;font-weight:800}.form-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:12px}.form-grid.two{grid-template-columns:repeat(2,1fr)}.incoming{display:grid;grid-template-columns:1fr 2fr .8fr;gap:8px;margin:8px 0}.inline-form{grid-template-columns:minmax(240px,1fr) auto;align-items:end;max-width:620px}.check{display:flex!important;align-items:center;gap:8px;font-weight:500!important;color:var(--muted)}.check input,.copy-option input{width:auto;margin:0}.status-line{margin:14px 0;color:var(--muted);font-size:12px}
.toolbar{display:grid;grid-template-columns:minmax(220px,2fr) 1fr 1fr;gap:8px;margin-bottom:4px}.toolbar input,.toolbar select{margin:0}.copy-card{margin:0;border-top:1px solid var(--line);background:transparent}.copy-card:first-of-type{border-top:0}.copy-card summary{display:flex;align-items:center;justify-content:space-between;gap:18px;padding:15px 2px;cursor:pointer;list-style:none}.copy-card summary::-webkit-details-marker{display:none}.copy-card summary span,.copy-card summary small{display:block}.copy-card summary b{font-size:14px}.copy-card summary small{margin-top:3px;color:var(--muted);font-size:11px}.copy-card summary strong{font-size:13px;font-variant-numeric:tabular-nums}.copy-card form{margin:0 0 14px;padding:16px;border:1px solid var(--line);border-radius:8px;background:#f8f7f3}.copy-card output{margin-left:8px;color:var(--accent);font-size:12px;font-weight:700}
.report{display:grid;grid-template-columns:minmax(150px,1.1fr) minmax(180px,2fr) auto auto;gap:10px;align-items:center;padding:12px 0;border-top:1px solid var(--line)}.report:first-child{border-top:0}.report span{color:var(--muted);font-size:13px}.report button{min-height:32px;padding:5px 9px;background:transparent;color:var(--ink);border-color:var(--line-strong);font-size:12px}.report>div{grid-column:1/-1;color:var(--muted)}
.cost{display:grid!important;grid-template-columns:minmax(180px,2fr) 1fr .8fr auto;align-items:end;gap:10px;padding:12px 0;border-top:1px solid var(--line)}.cost:first-child{border-top:0}.cost input{margin:0}.copy-option{margin:0;padding:9px 10px;border-top:1px solid var(--line);font-weight:500}.copy-option:first-child{border-top:0}.copy-option:has(input:disabled){color:var(--muted)}
#cost-review-list>p,#removal-list>p,#invite-list>p,#audit-list>p{margin:0;padding:11px 0;border-top:1px solid var(--line)}#cost-review-list>p:first-of-type,#removal-list>p:first-of-type,#invite-list>p:first-of-type,#audit-list>p:first-of-type{border-top:0}#cost-review-list button,#removal-list button,#invite-list button{min-height:30px;margin:3px 0 3px 6px;padding:4px 8px;background:transparent;color:var(--ink);border-color:var(--line-strong);font-size:12px}
.warning{margin:18px 0 0;padding:12px 14px;border-left:3px solid var(--danger);background:#f8eee9;color:#6e3428}.danger-zone{margin-top:30px;padding-top:24px;border-top:1px solid #dfc2b8}.danger-zone h3{color:var(--danger)}.danger-zone p{color:var(--muted)}pre{max-width:100%;overflow:auto;padding:14px;border:1px solid var(--line);border-radius:7px;background:#f5f4f0;font:12px/1.5 ui-monospace,SFMono-Regular,Menlo,monospace}.button+*{margin-left:6px}
.app-footer{max-width:1120px;margin:0 auto;padding:24px 22px 42px;border-top:1px solid var(--line);display:flex;justify-content:space-between;align-items:center;gap:28px;color:var(--muted);font-size:12px}.app-footer strong{color:var(--ink)}.app-footer p{margin:4px 0}.bgg-credit img{display:block;width:158px;max-width:38vw;height:auto}
.auth{width:min(430px,calc(100% - 28px));margin:7vh auto}.auth-brand{justify-content:center;margin-bottom:24px}.card{background:var(--surface);border:1px solid var(--line);border-radius:12px;padding:28px}.card>p:not(.section-label){color:var(--muted)}.card form{display:grid;gap:12px;margin-top:20px}.card label{font-size:13px;font-weight:700}.card aside{margin:14px 0;padding:11px 12px;border:1px solid #cbd9ce;border-radius:7px;background:var(--accent-soft)}.card details{margin-top:20px;padding-top:16px;border-top:1px solid var(--line)}.card summary{cursor:pointer;color:var(--muted);font-size:13px}.note{text-align:center;color:var(--muted);font-size:12px}
@media(max-width:760px){.topbar-inner{padding:0 14px;gap:16px}.brand strong{display:none}.primary-nav{gap:15px;overflow:auto}.primary-nav a,.quiet{font-size:12px}.topbar form{display:none}.app-shell{padding:30px 14px 46px}.page-head h1{font-size:31px}.tool-tabs{margin-left:-2px;margin-right:-2px}.surface{padding:18px}.metrics{grid-template-columns:1fr 1fr}.metrics article:last-child{grid-column:1/-1}.toolbar,.form-grid,.form-grid.two,.inline-form{grid-template-columns:1fr}.incoming{grid-template-columns:1fr}.report{grid-template-columns:1fr auto}.report span{grid-column:1/-1;grid-row:2}.report>div{grid-row:auto}.cost{grid-template-columns:1fr}.split-heading,.account-actions{flex-direction:column}.copy-card form{padding:13px}.app-footer{margin:0 14px;padding-left:0;padding-right:0;align-items:flex-start;flex-direction:column}.card{padding:22px}}
@media(max-width:430px){.primary-nav a[href="/app/account"]{display:none}.metrics{grid-template-columns:1fr}.metrics article:last-child{grid-column:auto}.page-head{margin-bottom:20px}.page-head h1{font-size:29px}.tool-tabs a{padding-left:10px;padding-right:10px}.copy-card summary{align-items:flex-start}.copy-card summary strong{white-space:nowrap}}
`;