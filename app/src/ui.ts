export type AppUser = { email: string; role: string };
const nav = [["library","Library"],["missing-prices","Missing prices"],["import","Import"],["trades","Trades"],["picker","Game picker"],["matcher","List matcher"]] as const;
export const escapeHtml=(v:string)=>v.replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"})[c]??c);

export function signInPage(message=""){return page("Sign in",`<main class="auth"><a class="brand" href="https://boardgames.planitnow.us/"><i>••<br>••</i><b>BoardGame<span>Engine</span></b></a><section class="card"><small>INVITATION-ONLY EARLY ACCESS</small><h1>Welcome back.</h1><p>Sign in with the email address that received your invitation.</p>${message?`<aside>${escapeHtml(message)}</aside>`:""}<form method="post" action="/auth/request"><label>Email address<input name="email" type="email" autocomplete="email" required></label><button>Email me a sign-in link</button></form><details><summary>I have a sign-in code</summary><form method="post" action="/auth/code"><label>Email address<input name="email" type="email" required></label><label>Sign-in code<input name="code" autocomplete="one-time-code" placeholder="ABCD-2345" required></label><button>Sign in with code</button></form></details></section><p class="note">Opening an emailed link does not use it. You confirm before signing in.</p></main>`)}
export function confirmPage(token:string){return page("Confirm sign-in",`<main class="auth"><a class="brand" href="https://boardgames.planitnow.us/"><i>••<br>••</i><b>BoardGame<span>Engine</span></b></a><section class="card"><small>ONE SAFE FINAL STEP</small><h1>Confirm sign-in.</h1><p>This link has not been consumed. Continue only if you requested it.</p><form method="post" action="/auth/confirm"><input type="hidden" name="token" value="${escapeHtml(token)}"><button>Continue signing in</button></form></section></main>`)}

export function appPage(user:AppUser,section="library"){
 const title=nav.find(x=>x[0]===section)?.[1]??(section==="invitations"?"Invitations":"Library");
 const links=nav.map(([s,l])=>`<a href="/app/${s}"${s===section?' aria-current="page"':""}>${l}</a>`).join("")+(user.role==="admin"?`<a href="/app/invitations"${section==="invitations"?' aria-current="page"':""}>Invitations</a>`:"");
 const content=sectionContent(section,title);
 return page(title,`<div class="shell"><aside><a class="brand" href="/app"><i>••<br>••</i><b>BoardGame<span>Engine</span></b></a><nav>${links}</nav><footer><small>Signed in as</small><b>${escapeHtml(user.email)}</b><form method="post" action="/auth/logout"><button>Sign out</button></form></footer></aside><main class="dash"><header><div><small>YOUR COLLECTION</small><h1>${escapeHtml(title)}</h1></div><em>STAGING</em></header>${content}</main></div>`)}

function sectionContent(section:string,title:string){if(section==="library")return`<section class="welcome"><div><small>START WITH YOUR SHELF</small><h2>Bring in your BoardGameGeek collection.</h2><p>Upload your collection CSV to see owned copies, known costs, and games waiting for a price.</p><a class="button" href="/app/import">Import collection CSV →</a></div><div class="books">▮▮▮▮▮</div></section><section class="metrics" id="metrics"><article><small>OWNED COPIES</small><b>—</b><span>Import to begin</span></article><article><small>KNOWN COST</small><b>—</b><span>Your ledger currency</span></article><article><small>PRICE COVERAGE</small><b>—</b><span>Unknown stays unknown</span></article></section><section class="next"><h2>What happens next</h2><ol><li><b>Upload privately.</b> Review recognized fields before saving.</li><li><b>Check the import.</b> Blank quantities create no owned copies.</li><li><b>Complete costs.</b> Blank prices enter your missing-price queue.</li></ol></section>`;if(section==="import")return tool("Import collection",`<form id="import-form"><label>BoardGameGeek collection CSV<input name="file" type="file" accept=".csv,text/csv" required></label><button>Review import</button></form><div id="import-result"></div>`);if(section==="missing-prices")return tool("Missing-price queue",`<p>Unknown prices remain distinct from explicit zero-cost copies.</p><div id="missing-list">Loading…</div>`);if(section==="invitations")return tool("Invite a collector",`<form id="invite-form"><label>Email address<input name="email" type="email" required></label><button>Send invitation</button></form><div id="invite-list">Loading…</div>`);if(section==="trades")return tool("Record a trade",`<div id="copy-list">Loading owned copies…</div><form id="trade-form"><label>Outgoing copies (one per line: copy-id | title | cost in minor units)<textarea name="outgoing" rows="4"></textarea></label><label>Incoming games (one per line: BGG-id | title | relative weight)<textarea name="incoming" rows="4" required></textarea></label><label>Shipping<input name="shipping" type="number" min="0" step=".01" value="0"></label><label>Additional cash<input name="cash" type="number" min="0" step=".01" value="0"></label><button>Preview and record equal/weighted allocation</button></form><p><a href="/api/trades/export">Export for-trade Markdown</a> · <a href="/api/trades/export?format=csv">CSV</a></p><pre id="trade-result"></pre>`);if(section==="picker")return tool("Fixture-driven game picker",`<form id="picker"><label>Players<input name="players" type="number" min="1" value="4"></label><label>Minutes<input name="minutes" type="number" min="1" value="90"></label><button>Rank fixture candidates</button></form><div id="picker-result"></div>`);if(section==="matcher")return tool("Match a pasted list",`<form id="matcher"><label>Game names or BGG links<textarea name="lines" rows="10" required></textarea></label><button>Match against imported games</button></form><div id="match-result"></div>`);return`<section class="empty"><small>FOUNDATION READY</small><h2>${escapeHtml(title)} is next.</h2></section>`}
const tool=(title:string,inside:string)=>`<section class="empty tool"><h2>${escapeHtml(title)}</h2>${inside}</section>`;
function page(title:string,body:string){return`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${escapeHtml(title)} — BoardGameEngine</title><style>${css}</style></head><body>${body}<script>${client}</script></body></html>`}
const client=`const q=s=>document.querySelector(s),api=async(u,o={})=>{const r=await fetch('/api'+u,o),j=await r.json();if(!r.ok)throw Error(j.error||'Request failed');return j};
if(q('#metrics'))api('/dashboard').then(d=>{const s=d.summary,c=Number(s.copies||0),p=Number(s.priced||0);q('#metrics').innerHTML='<article><small>OWNED COPIES</small><b>'+c+'</b><span>active</span></article><article><small>KNOWN COST</small><b>'+(Number(s.total||0)/100).toFixed(2)+'</b><span>known total</span></article><article><small>PRICE COVERAGE</small><b>'+(c?Math.round(p/c*100):0)+'%</b><span>'+p+' priced</span></article>'});
if(q('#import-form'))q('#import-form').onsubmit=async e=>{e.preventDefault();const d=await api('/imports/preview',{method:'POST',body:new FormData(e.target)});window.preview=d.rows;const multi=d.rows.map((r,i)=>({r,i})).filter(x=>x.r.quantity>1);q('#import-result').innerHTML='<p>'+d.summary.rows+' rows · '+d.summary.copies+' copies · '+d.summary.unknownCosts+' unknown costs · '+d.summary.warnings+' warnings</p>'+multi.map(x=>'<label>'+x.r.name+' has quantity '+x.r.quantity+'<select class="quantity-choice" data-index="'+x.i+'"><option value="">Choose…</option><option value="one">Import one copy</option><option value="split">Split into '+x.r.quantity+' copies</option></select></label>').join('')+'<button id="confirm-import" '+(d.rows.some(r=>r.warnings.some(w=>!w.includes('Quantity above')))?'disabled':'')+'>Confirm import</button><pre>'+JSON.stringify(d.rows.slice(0,20),null,2)+'</pre>';q('#confirm-import')?.addEventListener('click',async()=>{let rows=[];for(const r of window.preview){if(r.quantity>1){const choice=q('[data-index="'+window.preview.indexOf(r)+'"]').value;if(!choice)return alert('Choose how to handle every extra quantity.');if(choice==='one')rows.push({...r,quantity:1,warnings:[]});else for(let n=0;n<r.quantity;n++)rows.push({...r,collId:r.collId+'-'+(n+1),quantity:1,priceMinor:r.priceMinor===null?null:Math.floor(r.priceMinor/r.quantity)+(n<r.priceMinor%r.quantity?1:0),warnings:[]})}else rows.push(r)}await api('/imports/confirm',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({rows})});location='/app'})};
if(q('#missing-list'))api('/missing-prices').then(rows=>q('#missing-list').innerHTML=rows.length?rows.map(x=>'<form class="cost" data-id="'+x.id+'"><b>'+x.name+'</b><input name="amount" type="number" min="0" step=".01" required><input name="currency" placeholder="currency"><button>Save</button></form>').join(''):'<p>Nothing is waiting for a price.</p>').then?.(()=>{})).then(()=>document.querySelectorAll('.cost').forEach(f=>f.onsubmit=async e=>{e.preventDefault();const o=Object.fromEntries(new FormData(f));await api('/copies/'+f.dataset.id+'/cost',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify(o)});f.remove()}));
if(q('#invite-form')){const load=()=>api('/invitations').then(x=>q('#invite-list').innerHTML=x.map(i=>'<p>'+i.email+' — '+i.status+(i.status==='pending'?' <button class="invite-action" data-id="'+i.id+'" data-action="resend">Resend</button> <button class="invite-action" data-id="'+i.id+'" data-action="revoke">Revoke</button>':'')+' <button class="invite-action" data-id="'+i.id+'" data-action="disable">Disable</button></p>').join(''));load();q('#invite-form').onsubmit=async e=>{e.preventDefault();await api('/invitations',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});e.target.reset();load()};q('#invite-list').onclick=async e=>{const b=e.target.closest('.invite-action');if(!b)return;await api('/invitations/'+b.dataset.id,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:b.dataset.action})});load()}}
if(q('#matcher'))q('#matcher').onsubmit=async e=>{e.preventDefault();const d=await api('/match',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});q('#match-result').innerHTML='<pre>'+JSON.stringify(d,null,2)+'</pre>'};
if(q('#picker'))q('#picker').onsubmit=async e=>{e.preventDefault();const f=Object.fromEntries(new FormData(e.target)),candidates=[{id:1,name:'Fixture Game',minPlayers:2,maxPlayers:4,minutes:60,weight:2.5,best:20,recommended:25,notRecommended:5},{id:2,name:'Divisive Game',minPlayers:1,maxPlayers:6,minutes:45,weight:2,best:12,recommended:8,notRecommended:20}];const d=await api('/recommend',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({players:+f.players,minutes:+f.minutes,minWeight:0,maxWeight:5,candidates})});q('#picker-result').innerHTML=d.map(x=>'<h3>'+x.name+'</h3><p>'+x.reason+'</p>').join('')};
if(q('#copy-list'))api('/copies').then(x=>q('#copy-list').innerHTML='<h3>Active copies</h3>'+x.map(c=>'<p><code>'+c.id+'</code> — '+c.name+' — '+(c.costMinor===null?'cost unknown':(c.costMinor/100).toFixed(2))+'</p>').join(''));
if(q('#trade-form'))q('#trade-form').onsubmit=async e=>{e.preventDefault();const f=Object.fromEntries(new FormData(e.target)),parse=(v)=>String(v).split(/\\n/).filter(Boolean).map(x=>x.split('|').map(y=>y.trim()));const outgoing=parse(f.outgoing).map(x=>({copyId:x[0],title:x[1],costMinor:+x[2]})),incoming=parse(f.incoming).map(x=>({bggId:+x[0],title:x[1],weight:+x[2]||1}));const d=await api('/trades',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({outgoing,incoming,shippingMinor:Math.round(+f.shipping*100),cashMinor:Math.round(+f.cash*100),currency:'',notes:''})});q('#trade-result').textContent=JSON.stringify(d,null,2)};`;
const css=`:root{--ink:#17231d;--paper:#f5f1e8;--green:#245f4a;--lime:#d6e682;--rust:#b55437;--line:#d7d1c4}*{box-sizing:border-box}body{margin:0;background:var(--paper);color:var(--ink);font:16px/1.55 system-ui}h1,h2{line-height:1.05;letter-spacing:-.04em}a{color:inherit}.brand{display:flex;gap:.7rem;align-items:center;text-decoration:none}.brand i{font:10px/9px monospace;color:white;background:var(--rust);padding:9px 7px;border-radius:5px}.brand span,small{color:var(--rust)}button,.button{border:0;border-radius:3px;background:var(--lime);padding:.85rem 1rem;font-weight:800;text-decoration:none;cursor:pointer}.auth{width:min(440px,calc(100% - 2rem));margin:7vh auto}.auth>.brand{justify-content:center;margin-bottom:2rem}.card{background:white;border:1px solid var(--line);padding:2.4rem;box-shadow:12px 12px #e2dbce}.card h1{font-size:2.8rem;margin:.4rem 0}.card form{display:grid;gap:.8rem;margin-top:1.5rem}.card label{font-weight:700}.card input{display:block;width:100%;padding:.8rem;margin-top:.35rem;font:inherit}.card aside{background:#eef3d4;padding:.8rem}.card details{margin-top:1.5rem;border-top:1px solid var(--line);padding-top:1rem}.note{text-align:center;color:#647067;font-size:.8rem}.shell{min-height:100vh;display:grid;grid-template-columns:245px 1fr}.shell>aside{height:100vh;position:sticky;top:0;background:var(--ink);color:white;padding:2rem 1.5rem;display:flex;flex-direction:column}.shell nav{display:flex;flex-direction:column;margin-top:3rem}.shell nav a{padding:.7rem;text-decoration:none;color:#cad2cd}.shell nav a[aria-current]{background:#304038;color:white}.shell footer{margin-top:auto;display:flex;flex-direction:column;border-top:1px solid #405047;padding-top:1rem;overflow-wrap:anywhere}.shell footer button{background:none;color:var(--lime);padding:.5rem 0}.dash{padding:4rem 5vw}.dash header{display:flex;justify-content:space-between}.dash h1{font-size:4rem;margin:.2rem 0 2rem}.dash header em{font-style:normal;background:#e7dca8;border-radius:2rem;padding:.35rem .8rem;height:min-content}.welcome{background:var(--green);color:white;padding:3rem;display:grid;grid-template-columns:2fr 1fr}.welcome small{color:var(--lime)}.welcome h2{font-size:2.3rem}.welcome p{margin-bottom:2rem}.books{font-size:4rem;color:var(--lime);align-self:center}.metrics{display:grid;grid-template-columns:repeat(3,1fr);margin-top:1.5rem}.metrics article{background:white;border:1px solid var(--line);padding:1.5rem}.metrics b,.metrics span{display:block}.metrics b{font-size:2.8rem}.metrics span{color:#68736b}.next{margin-top:3rem}.next ol{display:grid;grid-template-columns:repeat(3,1fr);gap:2rem;padding-left:1rem}.empty{background:white;padding:4rem;border:1px solid var(--line)}@media(max-width:760px){.shell{grid-template-columns:1fr}.shell>aside{height:auto;position:static}.shell nav{display:grid;grid-template-columns:1fr 1fr;margin-top:1rem}.dash{padding:2rem 1rem}.welcome{grid-template-columns:1fr}.books{display:none}.metrics,.next ol{grid-template-columns:1fr}.card{padding:1.5rem;box-shadow:none}}`;
