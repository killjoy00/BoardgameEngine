// BGG requires a legible linked “Powered by BGG” logo in public-facing XML API uses.
// Serve the standard small logo from BoardGameEngine itself so attribution does not
// depend on another project's repository or an extra image host.
export const POWERED_BY_BGG_LOGO = "/powered_by_BGG_01_SM.png";
